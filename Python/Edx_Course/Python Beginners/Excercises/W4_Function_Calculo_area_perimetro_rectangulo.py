# Write a function that accepts two positive integers which are the height and width of a rectangle and returns a list that contains the area and perimeter of that rectangle.
#
def funcion_promedio_lista(lista_entrada):
   print(lista_entrada)
   #area = (height * width)
   #perimetro = ((height + width) * 2)
   #calculo_lista = []
   #calculo_lista.append(int(area))
   #calculo_lista.append(int(perimetro))
   #return calculo_lista

# OJO SOLO FUNCION!!!
# Main Program #
lista_entrada=[1,2,3,4,5]
evalua_funcion_promedio_lista = funcion_promedio_lista(lista_entrada)
print(evalua_funcion_promedio_lista)
